using UnityEngine;
using System.Collections;

public class playMovie : MonoBehaviour {

	//public MovieTexture movieTexture;
	private float movieDuration;
	private float elapsedMovieTime;

	// Use this for initialization
	void Start () {
		/*movieDuration = movieTexture.duration;
		movieTexture.Play ();*/

	}
	
	// Update is called once per frame
	void FixedUpdate () {
		/*elapsedMovieTime += Time.deltaTime;

		if (elapsedMovieTime > movieDuration) 
		{
			movieTexture.Stop();
			movieTexture.Play ();
			elapsedMovieTime = 0;
		}*/

	}
}
